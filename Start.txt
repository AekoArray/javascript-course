лаба 42 
Кошарский Илья ?
JavaScript
WebStorm
PHPStorm(PHP)
	alert('hello'); выводит сообщение hello
можно [1, 2].forEach(alert)
	комментарии одна строка - //
несколько /* ...*/ 
cntrl (shift) / -комментирует 
	код будет работать по стандарту ES5 (скрипт будет работать по современной спецификации) ---- "use strict";
	Переменная
для объявления var
example var message = "hi";
переменные начинающиеся на _ приватные, то есть подчеркнутые
	Константы
констант нет, но если пишут заглавными буквами, то считают это константой 
пример: var COLOR_RED = "#F00";
	Типы данных
-number может быть и 6 и 6.78
-string нет симвоолов "" и '' не отличаются
-boolean true or false
-null у переменной значений нет
-underfined значение не присвоили для какой-то переменной var x; alert(x);
-object var user = { name: "Vasya" };
	как нам узнать что там храниться - typeof
результат typeof строка typeof 0 // "number"
			typeof function(){} //"function"
			typeof null // "object" это баг 
	новые термины
унарный- один аргумент. бинарный-два аргумента. операнд

var x =-1? y = 3;
alert(x + y); вроде бы бинарный минус
x = -x;
alert(x); // -1 применили унарный минус
	сложение строк, бинарный +
alert(2 + '1'); //"21"
	преобразвание к числу, унарный плюс +
alert(+1); //1
alert(+(1 - 2)); // -1
	Приоритеты 
унарный +
унарный -
умножение *
деленние /
сложение +
вычитание -
присваивание =
	Присваивание
a=b=c= 2 + 2; //a = b = c = 4
взятие остатка % 
5 % 2 = 1
инкремент/декремент: ++, --
постфиксный сначало вернется значение, потом изменится i++
префиксный сначало изменится значение потом вернется значение ++i
	сокращенная арифметика с присваинванием n += 5; //n = n + 5
оператор запятая
возвращается последнее значение var a = (5, 6); // a = 6;
		Операторы сравнения и логические значения
!= не равно
2 > 1 //true
2 == 1 //false
	Сравнение строк
alert('Б' > 'А'); // true
	Сравнение разных типов (будут стараться числом сравнить)
alert('2' > 1); //true , 2 > 1
=== знак эквивалентности, надо чтобы типы совпадали
null == 0 //false
null >= 0 // true
undefined(становится null) > 0 //false
		взаимодействие с пользователями
alert - модальное действие
prompt - модальное окно выводит, то есть требует пользователя что-то ввести
result = prompt(title, default);
Confirm
quetion = confirm(quetion);
	0, undefined, null = false
	1 = true
	оператор тернарный ?
acess = (age > 14) ? true : false;
		Логические операторы
|| или
&& и
! не
!0 // true
!!null // false
		Преобразование типов
строковое String(null) //"null"
численное Number("123") или +"123" // 123
логичекое !!"0" //true
outer: jksbdc
break outer;
для фуекций переменные видны только внутри, а в других случаях переменные видны везде

Домашнее задание: 



